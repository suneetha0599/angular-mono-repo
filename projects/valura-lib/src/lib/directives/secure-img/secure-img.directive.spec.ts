import { SecureImgDirective } from './secure-img.directive';

describe('SecureImgDirective', () => {
  it('should create an instance', () => {
    const directive = new SecureImgDirective();
    expect(directive).toBeTruthy();
  });
});
