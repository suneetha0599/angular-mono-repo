import { ContentMeasureDirective } from "../../../../valura-admin/src/app/directives/content-measure.directive";


describe('ContentMeasureDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentMeasureDirective();
    expect(directive).toBeTruthy();
  });
});
