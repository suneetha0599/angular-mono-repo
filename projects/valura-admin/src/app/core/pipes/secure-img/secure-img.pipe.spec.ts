import { SecureImgPipe } from './secure-img.pipe';

describe('SecureImgPipe', () => {
  it('create an instance', () => {
    const pipe = new SecureImgPipe();
    expect(pipe).toBeTruthy();
  });
});
